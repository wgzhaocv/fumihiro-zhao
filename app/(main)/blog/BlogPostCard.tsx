import { Post } from "@/sanity/schemas/post";

type BlogPostCardProps = {
  post: Post;
  views: number;
};
const BlogPostCard = ({}: BlogPostCardProps) => {
  return <></>;
};

export default BlogPostCard;
